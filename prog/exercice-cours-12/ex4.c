#include <stdio.h>
#include <stdlib.h>

int Main(int argc, char *argv[]()) {
	if(argv < 2) {
		printf("Erreur Argument manquant \n");
		exit(1);
	}

	char b[1000];

	if(fprintf(stdin, "%s", b) != 0) {
		printf()
	}
}
